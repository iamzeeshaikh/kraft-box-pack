---
import Seo from '../components/Seo.astro';
import JsonLd from '../components/JsonLd.astro';
import SiteHeader from '../components/SiteHeader.astro';
import SiteFooter from '../components/SiteFooter.astro';
import { organization, website, graph } from '../lib/schema';
import '../styles/global.css';

import WhatsAppButton from '../components/WhatsAppButton.astro';
interface Props {
  title: string;
  description: string;
  path: string;
  noindex?: boolean;
  canonical?: string;
  image?: string;
  /** Page-specific JSON-LD nodes, merged into the site-wide @graph. */
  schema?: unknown[];
}

const {
  title,
  description,
  path,
  noindex = false,
  canonical,
  image,
  schema = [],
} = Astro.props;

const document = graph([organization(), website(), ...schema]);
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    {/* The kraft-bag mark from the logo artwork. A PNG set rather than an SVG
        because the logo is supplied as raster; .ico is kept so that browsers
        and crawlers requesting /favicon.ico by convention are answered. */}
    <link rel="icon" href="/favicon.ico" sizes="any" />
    <link rel="icon" href="/favicon-32.png" type="image/png" sizes="32x32" />
    <link rel="icon" href="/favicon-192.png" type="image/png" sizes="192x192" />
    <link rel="apple-touch-icon" href="/apple-touch-icon.png" />
    <meta name="theme-color" content="#1f7a2b" />
    <Seo
      title={title}
      description={description}
      path={path}
      noindex={noindex}
      canonical={canonical}
      image={image}
    />
    <JsonLd data={document} />
  </head>
  <body>
    <a href="#main" class="skip-link">Skip to content</a>
    <SiteHeader />
    <main id="main">
      <slot />
    </main>
    <SiteFooter />

    {/* ZeeOps live chat. Loaded last and deferred so it never blocks render;
        its origin is allow-listed in the CSP in astro.config.mjs. */}
    <script src="https://chat.zeeops.dev/widget.js?siteId=kraftboxpack" defer is:inline></script>
      <WhatsAppButton />
  </body>
</html>
